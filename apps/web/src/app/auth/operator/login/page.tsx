import { FormMasuk } from "@/components/FormMasuk";

export default function Page() {
  return <FormMasuk peran="OPERATOR" judul="Masuk sebagai Operator" keterangan="Tinjau legalitas Tenant dan putuskan klaim mutu." />;
}
